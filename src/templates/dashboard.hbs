<div class="header">
  <h2>
  {{#if n}}
    {{n}}
  {{else}}
    {{d}}
  {{/if}}
  </h2>
</div>
<div class="body">
  <div class="field left">
    <label>People</label>
    <span>{{us}}</span>
  </div>
  <div class="field right">
    <label>Projects</label>
    <span>{{pc}}</span>
  </div>
</div>

<div class="footer">
  <span>{{parseDate t}}</span>
</div>

